当前正在进行的任务是把外部标注链路收尾，并把结果接回主仓库，具体分两条线：

  - baseline_quality：LongBench 外部标注已经完成，外部夹具也已经导出。当前结果是
    100 条，风险分布 20 / 20 / 60，可直接用于后续导入和 smoke 运行。

  - baseline_session：ShareGPT 外部标注进程已经结束，当前正在检查结果是否完整可用。
    前面的 OOM 已经处理过，候选集也收紧到了能稳定运行的范围。现在重点是确认
    baseline_session_external.jsonl 是否成功导出，并核对关键统计和索引连续性。

这个任务完成后的后续任务，顺序应该是：

  1. 检查 ShareGPT 外部标注结果是否成功导出 baseline_session_external.jsonl，
     并核对 session 数、turn 数、风险分布、arrival_index 和 turn_index 连续性。

  2. 如果 ShareGPT 导出成功，把两个外部夹具都导入主仓库固定位置：
      - baseline_quality_external.jsonl
      - baseline_session_external.jsonl

  3. 先分别做一次导入校验和小规模预检，确认 loader、配置和 schema 都没问题。
  
  4. 跑 baseline_quality 的正式 smoke，检查五条 baseline 是否分开、质量和显存关系
     是否合理。

  5. 跑 baseline_session 的正式 smoke，检查 session 语义、pressure 事件、
     resident/global resident 演化是否完整。

  6. 最后一步：拿到可用的 baseline smoke 表，也就是 baseline_quality 和
     baseline_session 两张最终 smoke 表。
