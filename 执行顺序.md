现在项目已实现的实际顺序是：

  1. 先读配置，确定这次跑哪些 profiles / policies / epsilon / delta / memory_budget。
  2. 先跑 build_profile_table，生成真实的 profile 测量表。
  3. 再校验 profile 表是否合法，必须是 measured=True。
  4. 然后对每个预算组合跑 run_policies。
  5. policy 先选 profile。
  6. backend 再按 session_id + turn_index 回放这条请求的测量结果，并更新会话缓存状态。
  7. 最后 MetricCollector 汇总成 summary。
  8. 再画四张图。

  一句话：先测 profile，再用 measured replay 跑 policy，最后汇总和画图

实验设计中的设计顺序是：
  1. Profile 层先离线跑
      - 对每个 request + profile 做真实测量，产出 quality_loss / ttft /
        kv_memory / resident_memory 等表。

      - 这一步只负责“把候选动作的代价测出来”，不做在线决策。

  2. Calibration 层读 profile 表
      - 用 profile 表里的历史测量，做 QRP -> CG 的校准。
      - QRP 先预测某个请求在某个 profile 下的质量损失。
      - CG 再把点预测变成风险上界，筛出“安全 profile 集合”。

  3. Policy / STC 层在线选动作
      - STC 在安全集合里选一个最合适的 profile。
      - 选择时会同时看显存预算、TTFT、hysteresis、min residency、exact fallback。
      - 如果没有安全的近似 profile，就退回 full / offload / recompute。

  4. Backend 层真正执行
      - backend 按 session_id + turn_index 回放或执行这个动作。
      - 它维护会话级 cache_state，更新当前 profile、累计 KV、resident KV、
        offload / drop 状态。

  5. AAL 层做审计和漂移修正
      - 少量请求会被 shadow audit 到 full-KV。
      - 观测到漂移后，更新校准阈值，让 coverage 重新收回来。

  6. Metrics 层最后汇总
      - 统计 mean / p95 / violation / fallback / overhead / memory。
      - 再把结果画成 summary 图。

  一句话：profile 先测，calibration 再校准，policy 选动作，backend 执行并维护会话
  状态，AAL 审计修正，metrics 汇总。