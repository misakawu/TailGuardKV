Baseline Smoke 会话级重构执行计划
For agentic workers: REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (- [ ]) syntax for tracking.

Goal: 将 Profile 层的测量颗粒度从“单请求”重构为“会话累积”，使 Pilot 阶段的显存预算能够真正触发 Profile 过滤，从而让 Baseline Smoke 产生有区分度的指标曲线。

Architecture: Profile 层只测本轮新增 KV（incremental），Backend 在回放时按 session 累积；Policy 层决策时基于会话累积 KV 判断预算；允许会话内切换 profile；数据源切换为 ShareGPT 多轮会话数据集。

Tech Stack: Python 3.10+, pandas, ShareGPT 数据集, Qwen2.5-7B, transformers.

Global Constraints
Profile 表查询键：(session_id, turn_index, profile)

CSV 新增列：session_id, turn_index, kv_cumulative_mib, kv_incremental_mib

预算过滤条件：kv_cumulative_mib <= memory_budget_mib

会话内允许切换 profile，切换时重新计算该会话的 KV（从第一轮开始用新 profile 重算）

数据源：ShareGPT（需加载并解析 conversation 结构）

策略集合：5 个 Baseline（full_lru, static_best, static_safe, utility_dynamic, uncalibrated_dynamic）

Task 1: 扩展数据类型，支持会话级语义
任务解释
当前 Request 和 CacheState 只描述单个请求。要支持会话累积，需要让 Request 携带会话归属信息，让 CacheState 能够跟踪每个会话的累积 KV 状态。这是所有后续重构的数据基础。

Files:

Modify: core_types.py

Modify: data_utils.py

Interfaces:

Produces: Request 增加 session_id, turn_index, is_session_start, is_session_end

Produces: CacheState 增加 session_cache: dict[str, dict[str, float]]，键为 (session_id, profile)，值为累积 KV

□ Step 1: 扩展 Request 类
text
Request 类（修改）：
  现有字段：request_id, task, prompt, reference, metadata
  新增字段：
    session_id: str | None = None       # 会话唯一标识
    turn_index: int = 0                 # 该请求在会话中的轮次（从 0 开始）
    history_prompt: str | None = None   # 本轮之前的完整对话历史（用于构造累积 prompt）
  新增属性：
    effective_prompt: str               # history_prompt + prompt（实际输入模型的文本）
    is_session_start: bool              # turn_index == 0
    is_session_end: bool                # metadata 中标记（如 "last_turn": True）
□ Step 2: 扩展 CacheState 类
text
CacheState 类（修改）：
  现有字段：resident_memory_mib, objects
  新增字段：
    session_kv: dict[str, dict[str, float]] = {}
      # 结构：{session_id: {profile_name: cumulative_kv_mib}}
    session_profile: dict[str, str] = {}
      # 结构：{session_id: current_profile_name}（用于会话内一致性追踪）
    session_turn: dict[str, int] = {}
      # 结构：{session_id: last_turn_index}

  新增方法：
    get_cumulative_kv(session_id, profile) -> float:
      返回该会话在指定 profile 下的累积 KV
    set_cumulative_kv(session_id, profile, kv_mib) -> None:
      设置该会话在指定 profile 下的累积 KV
    get_current_profile(session_id) -> str | None:
      返回该会话当前使用的 profile
    set_current_profile(session_id, profile) -> None:
      设置该会话当前使用的 profile
□ Step 3: 扩展 Request 加载逻辑（data_utils.py）
text
load_requests(config) -> list[Request]:
  如果 data.source == "sharegpt":
    加载 ShareGPT 数据集
    对每个 conversation：
      提取 session_id
      遍历 turns，构造每个 turn 的 Request：
        request_id = f"{session_id}_turn_{turn_index}"
        prompt = 当前 turn 的 user 消息
        history_prompt = 前 N-1 轮的 user+assistant 拼接
        effective_prompt = history_prompt + "\n" + prompt
        turn_index = 当前轮次序号
    返回所有 Request 列表
  否则：
    保持原有单请求加载逻辑
□ Step 4: 提交变更
bash
git add core_types.py data_utils.py
git commit -m "feat: add session semantics to Request and CacheState"
Task 2: 重构 Profile 层，支持增量 KV 测量
任务解释
Profile 层不再测量“这个请求完整 KV 有多大”，而是测量“这个请求在已有会话 KV 基础上新增了多少 KV”。增量测量是“会话累积由 Backend 动态叠加”这一设计的前提。同时，Profile 层需要支持“指定 profile 重新计算整个会话的 KV”的能力，以支持会话内切换 profile。

Files:

Modify: profiles/base.py

Modify: profiles/full.py, profiles/kivi.py, profiles/h2o.py

Create: profiles/session_kv_utils.py（新增工具模块）

Interfaces:

Produces: ProfileMeasurement 新增字段 kv_incremental_mib, kv_cumulative_mib, session_id, turn_index

Produces: ProfileAdapter.profile() 支持 cache_state 参数（可选），传入会话当前 KV 状态

□ Step 1: 扩展 ProfileMeasurement 类（core_types.py）
text
ProfileMeasurement 类（修改）：
  现有字段：request_id, profile, adapter, ok, measured, output_text, error,
            latency_ms, ttft_ms, peak_memory_mib, resident_memory_mib,
            quality_score, quality_loss, extra
  新增字段：
    session_id: str | None = None
    turn_index: int = 0
    kv_incremental_mib: float | None = None   # 本轮新增 KV 大小
    kv_cumulative_mib: float | None = None    # 该会话在此 profile 下的累积 KV
□ Step 2: 创建 profiles/session_kv_utils.py
text
功能：管理会话 KV 累积状态

函数 build_session_prompt(conversation_history: list[dict], current_turn: int) -> str:
  输入：该会话所有 turn 的列表，当前轮次索引
  输出：拼接后的完整 prompt（包含所有历史上下文）

函数 get_incremental_kv(adapter, request, profile, existing_kv_state) -> float:
  输入：ProfileAdapter 实例、Request、profile 名称、已有 KV 状态（KV cache 对象）
  输出：本轮新增的 KV 大小（MiB）
  实现：
    1. 如果 existing_kv_state 为空：
       用 profile 运行请求，测量 KV 大小（就是累积量）
       返回 0（没有增量，因为这是第一轮）
    2. 如果 existing_kv_state 非空：
       在已有 KV 上追加当前请求
       测量追加前后的 KV 差 = 增量
       返回增量

函数 get_cumulative_kv(adapter, request, profile, existing_kv_state) -> float:
  输入：同上
  输出：累积 KV 大小（MiB）
  实现：
    1. 如果 existing_kv_state 为空：
       用 profile 运行请求，测量 KV 大小
    2. 如果 existing_kv_state 非空：
       在已有 KV 上追加当前请求
       测量追加后的总 KV 大小
□ Step 3: 修改 ProfileAdapter.profile() 接口
text
ProfileAdapter.profile(request, profile_name, dry_run=True, cache_state=None):
  输入增加了可选参数 cache_state（CacheState 对象）

  伪代码：
    如果 cache_state 不为空且 request.session_id 非空：
      从 cache_state 获取该会话在该 profile 下的已有 KV 状态
      用 profile 运行 request，测量：
        - kv_incremental = 追加后的总 KV - 已有 KV
        - kv_cumulative = 追加后的总 KV
      更新 cache_state 中的累积 KV
      返回 ProfileMeasurement（含 kv_incremental 和 kv_cumulative）
    否则：
      按原逻辑测量单请求 KV
      返回 ProfileMeasurement（kv_incremental = None, kv_cumulative = 峰值 KV）
□ Step 4: 提交变更
bash
git add profiles/base.py profiles/session_kv_utils.py core_types.py
git commit -m "feat: profile layer supports incremental KV measurement with session state"
Task 3: 重构 Profile 表生成，接入 ShareGPT 会话数据
任务解释
Profile 表生成需要从 ShareGPT 加载会话数据，按会话顺序逐轮测量每个 profile 的增量 KV 和累积 KV，并将所有结果写入带 session_id 和 turn_index 的 CSV 中。这是整个重构的数据源头。

Files:

Modify: run_build_profile_table.py

Modify: data_utils.py（增加 ShareGPT 加载）

Create: scripts/build_sharegpt_profile_table.py（或扩展现有脚本）

Interfaces:

Consumes: ShareGPT 数据集（JSON 格式，含 conversations）

Produces: out/profile_tables/sharegpt_session_profiles.csv（含 session_id, turn_index, kv_incremental, kv_cumulative）

□ Step 1: 在 data_utils.py 中增加 ShareGPT 加载函数
text
load_sharegpt_conversations(data_path: str) -> list[Conversation]:
  输入：ShareGPT 数据文件路径
  输出：Conversation 列表，每个包含 session_id 和 turns

  Conversation 结构：
    session_id: str
    turns: list[dict]  # 每个 turn 包含 user, assistant, turn_index

  伪代码：
    读取 JSON 文件
    对每个 conversation：
      提取 id 或生成 session_id
      对每个对话轮次：
        提取 user 消息和 assistant 消息
        构造 turn 记录
    返回会话列表
□ Step 2: 修改 run_build_profile_table.py 增加会话模式
text
build_profile_table(args):
  如果 args.session_mode:
    加载 ShareGPT 会话数据
    对每个会话：
      初始化 cache_state = CacheState()
      对会话中的每个 turn：
        构建 Request（含 session_id, turn_index, history_prompt）
        对每个 profile：
          测量该 turn 在该 profile 下的增量 KV 和累积 KV
          记录到 ProfileMeasurement
          更新 cache_state
    输出 CSV（含 session_id, turn_index, kv_incremental, kv_cumulative）
  否则：
    按原逻辑生成单请求 Profile 表
□ Step 3: 提交变更
bash
git add data_utils.py run_build_profile_table.py
git commit -m "feat: profile table generation supports ShareGPT sessions"
Task 4: 重构 measured-replay Backend，支持会话级查表与累积
任务解释
Backend 不再只是简单查表，它需要：

按 (session_id, turn_index, profile) 查询 Profile 表

维护每个会话的累积 KV 状态

如果会话内切换 profile，重新计算该会话在切换后所有轮的累积 KV（或标记为“需要重建”）

Files:

Modify: backends/measured_replay.py

Interfaces:

Consumes: Profile 表（含 session_id, turn_index）

Produces: 累积 KV 状态，按会话维护

□ Step 1: 修改 MeasuredReplayBackend 的查表逻辑
text
MeasuredReplayBackend.run(requests, profiles):
  初始化 session_cache = {}  # session_id -> CacheState

  对每个请求：
    如果 request.session_id 非空：
      获取该会话的当前 CacheState
      如果该会话已有决策的 profile：
        用该 profile 查表
      否则：
        从 profiles 中选（由 Policy 决策）
      查询 Profile 表，键 = (session_id, turn_index, selected_profile)
      从 measurement 中读取：
        - kv_incremental = measurement.kv_incremental_mib
        - kv_cumulative = measurement.kv_cumulative_mib
      更新 session_cache[session_id] 的累积 KV
    否则：
      按原逻辑查表（单请求模式）
  返回 ProfileMeasurement 列表
□ Step 2: 提交变更
bash
git add backends/measured_replay.py
git commit -m "feat: measured-replay supports session-level lookup and cumulative KV tracking"
Task 5: 修改 StatsPolicy，预算过滤改用会话累积 KV
任务解释
Policy 层的预算过滤必须从“基于单请求峰值”改为“基于会话累积 KV”。这样，当会话累积 KV 超过预算时，该 profile 才会被过滤掉，从而触发策略切换。

Files:

Modify: policies/base.py

Interfaces:

Consumes: Request（含 session_id）, CacheState（含 session_kv）

Produces: 基于 kv_cumulative_mib 的预算过滤结果

□ Step 1: 修改 _memory_or_inf() 方法
text
StatsPolicy._memory_or_inf(profile, request, cache_state):
  如果 request.session_id 非空且 cache_state 非空：
    从 cache_state.session_kv[request.session_id] 获取该会话在该 profile 下的累积 KV
    如果存在，返回累积 KV
    如果不存在（该会话还没用这个 profile 测过），返回 inf
  否则：
    返回原 p95_peak_memory_mib
□ Step 2: 修改 _candidate_profiles() 方法
text
StatsPolicy._candidate_profiles(request, cache_state, include_exact):
  对每个 profile：
    获取该 profile 的累积 KV（通过 _memory_or_inf）
    如果累积 KV <= memory_budget_mib：
      该 profile 为候选
  返回候选列表
□ Step 3: 提交变更
bash
git add policies/base.py
git commit -m "feat: policy budget filtering uses session cumulative KV instead of per-request peak"
Task 6: 修改 run_policies，传递会话状态并在会话内跟踪 profile
任务解释
run_policies.py 负责驱动整个策略执行流程。它需要确保：

同一会话的请求共享同一个 CacheState 对象

会话内切换 profile 时，更新会话的当前 profile

如果切换 profile，需要重新计算累积 KV（从第一轮开始）

Files:

Modify: run_util/run_policies.py

Interfaces:

Consumes: 请求列表（含 session_id）

Produces: PolicyRunRecord 列表

□ Step 1: 修改请求分组逻辑
text
run_policies(args):
  加载 Profile 表
  加载请求列表（可能含 session_id）
  按 session_id 分组请求：
    如果 session_id 非空：
      同一 session 的请求放在一组
    如果 session_id 为空：
      每个请求单独成组

  对每个会话组：
    初始化 cache_state = CacheState()
    初始化 current_profile = None
    对组内每个请求（按 turn_index 排序）：
      Policy.decide(request, cache_state, device_state)
        内部会判断：如果 current_profile 为空或 budget 变化，可能切换 profile
      执行决策：
        如果切换了 profile：
          更新 current_profile
          从第一轮开始重新计算该会话的累积 KV（用新 profile）
        否则：
          在当前 profile 下继续累积
      记录 PolicyRunRecord
      更新 cache_state
□ Step 2: 提交变更
bash
git add run_util/run_policies.py
git commit -m "feat: run_policies tracks session state and supports in-session profile switching"
Task 7: 生成 ShareGPT Profile 表并重新跑 Pilot
任务解释
所有代码修改完成后，生成新的 Profile 表（基于 ShareGPT 会话），然后用这个新表重新跑 Pilot Smoke，验证预算过滤是否生效，TTFT 曲线是否出现有区分度的变化。

Files:

No code changes; execution step.

Produces: 新的 Profile 表、新的 Policy summary

□ Step 1: 生成 ShareGPT Profile 表
text
运行命令：
  python run_build_profile_table.py \
    --config configs/pilot.yaml \
    --session-mode \
    --data-source sharegpt \
    --output out/profile_tables/sharegpt_profiles.csv \
    --dry-run False
□ Step 2: 验证 Profile 表包含会话数据
text
检查 CSV 列：session_id, turn_index, kv_incremental, kv_cumulative
检查数值：同一会话的 kv_cumulative 应随 turn_index 递增
检查范围：kv_cumulative 应在 200–3000 MiB 范围（取决于会话长度）
□ Step 3: 重新跑 Pilot Smoke
text
运行命令：
  python run_experiment.py pilot-smoke-measured \
    --config configs/pilot.yaml \
    --measurements out/profile_tables/sharegpt_profiles.csv \
    --memory-budget-mib 500 \
    --epsilon 0.1 \
    --delta 0.05
□ Step 4: 验证预算过滤是否生效
text
检查 Policy 决策：
  - full_gpu 的累积 KV 是否在某些会话中 > 预算 → 被过滤
  - 策略是否在不同的会话/轮次中选择了不同的 profile
  - TTFT vs 预算曲线是否出现有区分度的变化
Task 8: 更新可视化，支持会话级指标曲线
任务解释
之前的可视化把 (budget, epsilon, delta) 混在一起画，导致看起来像周期波动。需要修改绘图逻辑，固定 ε/δ 组合，单独画 budget 曲线，并确保横轴是“有效预算范围”（20–2000 MiB，而非 1000–5000）。

Files:

Modify: visual/plot_summary.py

Interfaces:

Consumes: Policy summary CSV

Produces: 按 ε/δ 分组的 budget 曲线图

□ Step 1: 修改绘图逻辑
text
plot_summary(summary_csv, output_dir):
  读取 summary CSV
  筛选 section == "policy"
  对每个 (epsilon, delta) 组合：
    提取该组合下的所有 budget 数据
    对每个 policy：
      绘制 p95_ttft_ms vs budget_mib 曲线
      绘制 violation_rate vs budget_mib 曲线
      绘制 mean_kv_cache_memory_mib vs budget_mib 曲线
    保存为单独的 PNG 文件
  生成对比图（同一图显示所有 policy）
自检清单
检查项	预期状态
Profile 表含 session_id、turn_index、kv_cumulative	✅
kv_cumulative 随会话轮次递增	✅
full_gpu 累积 KV 在长会话中 > 预算	✅
预算过滤在会话级生效（不同会话/轮次过滤不同）	✅
策略决策随预算变化而切换 profile	✅
TTFT vs 预算曲线出现有区分度的变化	✅
绘图固定 ε/δ，单独显示 budget 影响	✅
计划完成。执行顺序：

text
Task 1 (数据类型) → Task 2 (Profile 增量测量) → Task 3 (ShareGPT Profile 表生成)
                                                      ↓
Task 4 (Backend 会话查表) → Task 5 (Policy 累积 KV 过滤) → Task 6 (run_policies 会话状态)
                                                      ↓
                                    Task 7 (重新跑 Pilot)
                                                      ↓
                                    Task 8 (更新可视化)