# 项目进度

## 当前状态

本轮“排除原 batch7 对应会话并重新运行诊断实验”已完成。此前启动的进程已正常结束，输出结果未发现报错。

## 已完成工作

- 扩展 `scripts/run_diagnostic_session_batches.py`，支持按 session 排除数据，并在过滤后重新按 session 构建批次。
- 增加过滤审计信息，记录源数据、排除数据和过滤后数据的 session/request 数量。
- 保证生成的 batch 配置继承原实验的 seed、profiles 和 policies，并正确写入各 batch 的 fixture、请求数与输出目录。
- 在 `tests/test_diagnostic_batch_runner.py` 中补充相关测试，覆盖：
  - 排除指定 session 后重新分批；
  - `--prepare-only` 模式生成过滤审计；
  - batch 配置与 manifest 保持一致。
- 完成排除原 batch7 对应会话后的诊断运行。

## 本次运行信息

输出目录：

`out/diagnostic_session25_excluding_original_batch7_v2/`

过滤审计结果：

- 源数据：27 个 session，135 个 request；
- 排除 session：`hybrid-session-014`、`hybrid-session-015`；
- 排除数据：2 个 session，10 个 request；
- 过滤后数据：25 个 session，125 个 request；
- 重新构建：12 个 batch；
- 分批规则：通常每批 2 个 session，最后一个 batch 包含 3 个 session；
- 实验标记：`diagnostic_only: true`。

本次运行覆盖 8 个 profile：

1. `full_gpu`
2. `kivi_4bit_residual32`
3. `kivi_4bit_residual64`
4. `kivi_2bit_residual32`
5. `kivi_2bit_residual64`
6. `h2o_heavy10_recent10`
7. `h2o_heavy15_recent15`
8. `h2o_heavy20_recent20`

## 结果状态

- 运行进程已正常结束；
- 输出结果已生成；
- 未发现运行报错；
- 过滤后的 fixture、12 组 batch fixture/config 和对应 batch 输出均位于上述输出目录；
- 当前阶段可视为完成。

## 后续建议

1. 汇总 12 个 batch 的指标，形成排除异常会话后�的统一结果表。
2. 将本次结果与未排除原 batch7 的结果进行对比，判断异常会话对总体指标的影响。
3. 若结果满足预期，整理并提交脚本、测试和进度文档；`out/` 是否纳入版本控制需按项目的数据管理约定处理。
